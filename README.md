Run with:

```bash
streamlit run app.py
```

The app now includes set-piece roles and archetypes, automatic coaching insights,
matplotlib/mplsoccer pitch visuals, and pre-match PDF report downloads from the
active filters.
